server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # 关键配置：如果找不到文件，就回退到 index.html，交给 React Router 处理
        try_files $uri $uri/ /index.html;
    }
}
